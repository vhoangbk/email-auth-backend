╔══════════════════════════════════════════════════════════════════╗
║        EMAIL AUTHENTICATION SYSTEM - QUICK REFERENCE             ║
╚══════════════════════════════════════════════════════════════════╝

📦 SETUP
┌──────────────────────────────────────────────────────────────────┐
│ 1. cp .env.example .env              # Copy env template         │
│ 2. # Edit .env with your settings                                │
│ 3. npx prisma generate               # Generate Prisma client    │
│ 4. npx prisma migrate dev            # Setup database            │
│ 5. npm run dev                       # Start server              │
│                                                                   │
│ OR use quick start:                                              │
│ ./start.sh                           # Automated setup & start   │
└──────────────────────────────────────────────────────────────────┘

🚀 COMMANDS
┌──────────────────────────────────────────────────────────────────┐
│ npm run dev              Start development server                │
│ npm run build            Build for production                    │
│ npm run type-check       Check TypeScript types                  │
│ npx prisma studio        Open database GUI                       │
│ npx prisma migrate dev   Create & apply migration               │
└──────────────────────────────────────────────────────────────────┘

📡 API ENDPOINTS
┌──────────────────────────────────────────────────────────────────┐
│ POST   /api/auth/register       Register new user               │
│ POST   /api/auth/login          Login & get JWT token           │
│ GET    /api/auth/verify         Verify email with token         │
│ POST   /api/auth/reset-password Request password reset          │
│ PUT    /api/auth/reset-password Reset password                  │
│ GET    /api/user/profile   🔒   Get user profile                │
│ PUT    /api/user/profile   🔒   Update user profile             │
│                                                                   │
│ 🔒 = Requires: Authorization: Bearer <JWT_TOKEN>                 │
└──────────────────────────────────────────────────────────────────┘

🔐 ENVIRONMENT VARIABLES (required in .env)
┌──────────────────────────────────────────────────────────────────┐
│ DATABASE_URL         MySQL connection string                     │
│ JWT_SECRET           Secret key for JWT (32+ chars)             │
│ SMTP_HOST           Email server host                            │
│ SMTP_PORT           Email server port (587)                      │
│ SMTP_USER           Email username                               │
│ SMTP_PASS           Email password (use App Password)            │
└──────────────────────────────────────────────────────────────────┘

📚 DOCUMENTATION
┌──────────────────────────────────────────────────────────────────┐
│ README.md            Complete setup & usage guide                │
│ AGENTS.md            Code style guide for AI agents              │
│ API_TESTING.md       API endpoint testing guide                  │
│ PROJECT_SUMMARY.md   Project overview & architecture             │
└──────────────────────────────────────────────────────────────────┘

🧪 QUICK TEST
┌──────────────────────────────────────────────────────────────────┐
│ # Register                                                        │
│ curl -X POST http://localhost:3000/api/auth/register \           │
│   -H "Content-Type: application/json" \                          │
│   -d '{"email":"test@example.com","password":"Test123456"}'      │
│                                                                   │
│ # Login                                                           │
│ curl -X POST http://localhost:3000/api/auth/login \              │
│   -H "Content-Type: application/json" \                          │
│   -d '{"email":"test@example.com","password":"Test123456"}'      │
└──────────────────────────────────────────────────────────────────┘

📂 PROJECT STRUCTURE
┌──────────────────────────────────────────────────────────────────┐
│ app/api/           API routes (auth, user)                       │
│ lib/               Utilities (auth, email, prisma)               │
│ prisma/            Database schema & migrations                  │
│ types/             TypeScript type definitions                   │
│ middleware.ts      Auth middleware for protected routes          │
└──────────────────────────────────────────────────────────────────┘

✨ FEATURES
┌──────────────────────────────────────────────────────────────────┐
│ ✅ User registration with email verification                     │
│ ✅ Secure login with JWT (7-day expiration)                      │
│ ✅ Password reset functionality                                  │
│ ✅ Protected API routes                                          │
│ ✅ Password hashing with bcrypt                                  │
│ ✅ TypeScript strict mode                                        │
│ ✅ Prisma ORM with MySQL                                         │
│ ✅ Security headers                                              │
└──────────────────────────────────────────────────────────────────┘

🔧 TROUBLESHOOTING
┌──────────────────────────────────────────────────────────────────┐
│ Issue: Can't connect to database                                 │
│ Fix: Check MySQL running & DATABASE_URL in .env                  │
│                                                                   │
│ Issue: Email not sending                                         │
│ Fix: Use Gmail App Password, check SMTP settings                 │
│                                                                   │
│ Issue: Type errors                                               │
│ Fix: Run `npx prisma generate` after schema changes              │
└──────────────────────────────────────────────────────────────────┘

🎯 NEXT STEPS
┌──────────────────────────────────────────────────────────────────┐
│ 1. Configure .env with real database & SMTP credentials          │
│ 2. Run database migrations                                       │
│ 3. Test API endpoints (see API_TESTING.md)                       │
│ 4. Build your frontend application                               │
│ 5. Add rate limiting for production                              │
│ 6. Configure CORS for your frontend domain                       │
│ 7. Deploy to production (Vercel, Railway, etc.)                  │
└──────────────────────────────────────────────────────────────────┘

💡 TIPS
┌──────────────────────────────────────────────────────────────────┐
│ • Use Mailtrap.io or Ethereal Email for testing emails           │
│ • Run `npx prisma studio` to view/edit database visually         │
│ • Check AGENTS.md for code style guidelines                      │
│ • JWT tokens expire after 7 days (configurable in lib/auth.ts)  │
│ • Password must have: 8+ chars, uppercase, lowercase, number     │
└──────────────────────────────────────────────────────────────────┘

Server: http://localhost:3000
Prisma Studio: http://localhost:5555
