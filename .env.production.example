# Production Environment Variables for Vercel
# Copy these to Vercel Dashboard → Settings → Environment Variables

# Database (PlanetScale, Railway, or other MySQL cloud)
DATABASE_URL="mysql://username:password@host.region.provider.com:3306/database_name?sslaccept=strict"

# JWT Secret - MUST be different from development
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="<your-production-secret-key-minimum-32-characters>"

# Application URL - Update with your actual Vercel URL after first deploy
NEXT_PUBLIC_APP_URL="https://your-project-name.vercel.app"

# SMTP Configuration (Gmail example)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-production-email@gmail.com"
SMTP_PASS="<your-gmail-app-password>"
SMTP_FROM="noreply@yourdomain.com"

# Optional: Custom domain URL (after setup)
# NEXT_PUBLIC_APP_URL="https://api.yourdomain.com"
